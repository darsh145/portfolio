---
import Layout from "../layouts/Layout.astro"
import Shelf from "@/components/Shelf"
---

<Layout>
    <main class="container mx-auto px-4 sm:px-6 md:px-8 py-4 sm:py-6 md:py-8">
        <div class="mb-4 sm:mb-6">
            <a href="/home" class="inline-flex items-center px-2 py-2 bg-gray-200 hover:bg-[#00bbff40] text-white rounded-[50%] transition-colors duration-200 back-button cursor-none">
                <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 448 512" class="h-5 w-5 sm:h-6 sm:w-6" fill="#000000">
                    <path d="M9.4 233.4c-12.5 12.5-12.5 32.8 0 45.3l160 160c12.5 12.5 32.8 12.5 45.3 0s12.5-32.8 0-45.3L109.2 288 416 288c17.7 0 32-14.3 32-32s-14.3-32-32-32l-306.7 0L214.6 118.6c12.5-12.5 12.5-32.8 0-45.3s-32.8-12.5-45.3 0l-160 160z"/>
                </svg>
            </a>
        </div>

        <div class=" flex justify-center items-center mt-4 mb-4 sm:mt-6 md:mt-8">
            <h2 class="flex justify-center text-center text-2xl sm:text-3xl md:text-4xl font-bold text-gray-600 font-reenie px-4">
                I am a Writer who Designs !!
            </h2>	
        </div>
        

        <!-- Shelves -->
    <div class="space-y-12">
      <Shelf client:load title="Blog Library" books={[
        { 
            img: "/blog1.png", 
            title: "Early Design Careers", 
            content: "",
        },
        { 
            img: "/blog1.png", 
            title: "Early Design Careers", 
        },
        { 
            img: "/blog1.png", 
            title: "Early Design Careers", 

        },
        { 
            img: "/blog1.png", 
            title: "Early Design Careers", 
        },
        // ... more blogs
        ]} />

      <Shelf title="Next Up" books={[
        
      ]} />

      <!-- <Shelf title="Finished" books={[
        { img: "/books/book5.png", title: "Steve Jobs", content: "Some text..." },
      ]} /> -->
    </div>

    </main>
</Layout>

<style>

    .font-reenie {
        font-family: 'Reenie Beanie', cursive;
    }

    .shelf {
        position: relative;
    }
</style>

<!-- <script type="text/jsx">
  import { useState } from "react";

  function Shelf({ title, books }) {
    const [open, setOpen] = useState(false);
    const [activeBook, setActiveBook] = useState(null);

    const openModal = (book) => {
      setActiveBook(book);
      setOpen(true);
    };

    const closeModal = () => setOpen(false);

    return (
      <div className="shelf">
        <h3 className="text-xl font-semibold text-gray-500 mb-3">{title}</h3>
        <div className="flex gap-6 items-end border-b-4 border-[#f4e1c1] pb-4 relative before:content-[''] before:absolute before:bottom-0 before:left-0 before:w-full before:h-2 before:bg-[#f4e1c1]">
          {books.map((book, index) => (
            <img
              key={index}
              src={book.img}
              alt={book.title}
              className="h-32 sm:h-40 cursor-pointer hover:-translate-y-2 transition-transform shadow-md rounded"
              onClick={() => openModal(book)}
            />
          ))}
        </div>

        {open && (
          <div className="fixed inset-0 z-50 flex items-center justify-center bg-black/70 backdrop-blur-sm">
            <div className="bg-white w-11/12 sm:w-3/4 md:w-1/2 p-6 rounded-xl relative max-h-[90vh] overflow-y-auto">
              <button
                onClick={closeModal}
                className="absolute top-3 right-3 text-gray-600 hover:text-black"
              >
                ✕
              </button>
              <h2 className="text-2xl font-bold mb-4">{activeBook.title}</h2>
              <p className="text-gray-700 leading-relaxed">{activeBook.content}</p>
            </div>
          </div>
        )}
      </div>
    );
  }
</script> -->